/**
 * Local credential plumbing for the embedded qBid runtime.
 *
 * <p>These classes expose the qBid operator credentials already provided by the desktop launcher
 * to the rest of the runtime without relying on process-wide static state.
 */
package tools.ceac.ai.modules.qbid.application.auth;

